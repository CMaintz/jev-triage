# Copy this to `.env` (git-ignored) and paste your key. Used only for the local
# live smoke test (test/live.test.ts) — in real use the Action reads the key from
# the `jev-api-key` input (a GitHub Actions secret), not from a file.
JEV_API_KEY=
# Optional: pin a model.
# JEV_MODEL=jev-1.13.0
